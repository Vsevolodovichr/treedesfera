# Xatosfera_Capture

## Depth Level 3

Depth Level 3 додає локальне створення depth-мап для 3D-перегляду фото туру. Вимкнути функцію можна через `VITE_DEPTH_ENABLED=false`. Модель кешується браузером через Transformers.js / IndexedDB cache.
